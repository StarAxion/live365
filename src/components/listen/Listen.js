import React from 'react';
import Typography from '@material-ui/core/Typography';

const Listen = () => {

  return (
    <Typography
      variant="h1"
      color="primary"
    >
      Listen
    </Typography>
  )
}

export default Listen;
