import React from 'react';
import Typography from '@material-ui/core/Typography';

const Listen = () => {

  return (
    <Typography
      variant="h1"
    >
      Listen
    </Typography>
  )
}

export default Listen;
